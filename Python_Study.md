# 个人学习笔记：Python 环境开发与基础实操

> **时间：** 2024年3月
> **目标：** 打通 GitHub 协作链路，掌握 Python 基础数据处理

## 一、 开发环境搭建（踩坑记录）

本周重点是把这套“套件”配好。说实话，GitHub 的连接折腾了挺久。

1. **Git & GitHub**：
   - 重点掌握了 `git clone`，把云端仓库同步到本地。
   - 配置了全局的 `user.name` 和 `user.email`，不然没法提交。
   - **心得：** 以后改了代码要记得“三部曲”：`add` -> `commit` -> `push`。

2. **编辑器工具**：
   - 使用 **Typora** 记录 Markdown 笔记（所见即所得，比 Word 好用多了）。
   - 安装了 **PyCharm 2025**，准备用来跑大规模的 Python 代码。

---

## 二、 Python 基础语法（核心点记录）

这几天主要过了一下 Python 的基本逻辑，跟以前接触过的语言不太一样，缩进非常严格。

- **数据类型**：除了基本的 String 和 Int，重点看了 **List（列表）** 和 **Dict（字典）**，这两个在处理 JSON 数据时用得最多。
- **循环控制**：练习了 `for` 循环和循环嵌套。
- **函数**：学会了如何定义 `def`，尝试把重复的代码封装起来。

---

## 三、 数据科学“三剑客”预研

根据作业要求，简单了解了后续要深入学习的库：

| 库名           | 主要用途                                 | 学习状态 |
| :------------- | :--------------------------------------- | :------- |
| **NumPy**      | 矩阵运算，特别是和线性代数相关的数组操作 | 了解中   |
| **Pandas**     | 读取 CSV 或 Excel，处理表格型数据        | 待上手   |
| **Matplotlib** | 把数据可视化，画折线图和散点图           | 待上手   |

---

## 四、 数学基础梳理

为了配合多智能体系统的学习，复习了几个核心数学概念：

1. **线性代数**：
   - 重点看了**矩阵乘法**。在 Python 里可以用 `numpy.dot()` 实现。
   - 理解了线性变换的基本意义：$y = Ax + b$。
2. **微积分**：
   - 复习了**多元微分/偏导数**，这对后续理解算法的梯度下降很有帮助。

---

## 五、 下周计划
- [ ] 完成 Anaconda 的虚拟环境配置。
